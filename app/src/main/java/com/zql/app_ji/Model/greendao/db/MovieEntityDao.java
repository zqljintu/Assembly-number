package com.zql.app_ji.Model.greendao.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.zql.app_ji.Bean.Entity.MovieEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "MOVIE_ENTITY".
*/
public class MovieEntityDao extends AbstractDao<MovieEntity, Long> {

    public static final String TABLENAME = "MOVIE_ENTITY";

    /**
     * Properties of entity MovieEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "Id", true, "_id");
        public final static Property Title = new Property(1, String.class, "title", false, "TITLE");
        public final static Property Movie_id = new Property(2, String.class, "movie_id", false, "MOVIE_ID");
    }


    public MovieEntityDao(DaoConfig config) {
        super(config);
    }
    
    public MovieEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MOVIE_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: Id
                "\"TITLE\" TEXT NOT NULL ," + // 1: title
                "\"MOVIE_ID\" TEXT NOT NULL );"); // 2: movie_id
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MOVIE_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, MovieEntity entity) {
        stmt.clearBindings();
 
        Long Id = entity.getId();
        if (Id != null) {
            stmt.bindLong(1, Id);
        }
        stmt.bindString(2, entity.getTitle());
        stmt.bindString(3, entity.getMovie_id());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, MovieEntity entity) {
        stmt.clearBindings();
 
        Long Id = entity.getId();
        if (Id != null) {
            stmt.bindLong(1, Id);
        }
        stmt.bindString(2, entity.getTitle());
        stmt.bindString(3, entity.getMovie_id());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public MovieEntity readEntity(Cursor cursor, int offset) {
        MovieEntity entity = new MovieEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // Id
            cursor.getString(offset + 1), // title
            cursor.getString(offset + 2) // movie_id
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, MovieEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTitle(cursor.getString(offset + 1));
        entity.setMovie_id(cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(MovieEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(MovieEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(MovieEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
